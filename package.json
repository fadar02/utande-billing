{
  "name": "utande-billing",
  "version": "1.0.0",
  "description": "Utande Smart Billing System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npx prisma db push --skip-generate --accept-data-loss && npx tsc",
    "start": "cd backend && node dist/index.js"
  }
}
